document.addEventListener('DOMContentLoaded', () => {
  // const simpleBarContent = document.querySelector('.center .simplebar-content');
  // simpleBarContent.classList.add('flex', 'flex-col', 'h-full', 'justify-end');
});